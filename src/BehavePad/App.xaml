<Application x:Class="BehavePad.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:BehavePad.Controls"
             xmlns:views="clr-namespace:BehavePad.Views"
             xmlns:vm="clr-namespace:BehavePad.ViewModels"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Controls.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <controls:BoolToVisibilityConverter x:Key="BoolToVisibility" />
            <controls:NullToVisibilityConverter x:Key="NullToVisibility" />
            <controls:EnumEqualsConverter x:Key="EnumEquals" />
            <controls:EnumToVisibilityConverter x:Key="EnumToVisibility" />
            <controls:SeverityBrushConverter x:Key="SeverityBrush" />
            <controls:SeverityTextConverter x:Key="SeverityText" />
            <controls:PercentConverter x:Key="Percent" />

            <DataTemplate DataType="{x:Type vm:OverviewViewModel}">
                <views:OverviewView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:TestViewModel}">
                <views:TestView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:LiveViewModel}">
                <views:LiveView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SetupViewModel}">
                <views:SetupView />
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
